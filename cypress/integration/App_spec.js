function getElement(selector) {
    return cy.get(`[data-test=${selector}`)
}

describe("Todo Application", () => {
    it("Adds a new todo", () => {
        
    })
    it("Deletes a todo", () => {

    })
    it("Modifies a todo", () => {

    })
}) 